apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gok
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: gok.local
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gok
            port:
              number: 80